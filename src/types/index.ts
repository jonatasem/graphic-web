export interface Comment {
    date: string;
    contact: string;
    rating: string;
    professional: string;
    unidad: string;
}

export interface Filters {
    unidad: string;
    professional: string;
    rating: string;
}